import React from "react";
import { FormContainer } from "./styled";

export const CovidTestForm = () => {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
      }}
    >
      <FormContainer></FormContainer>
    </div>
  );
};

export default CovidTestForm;
